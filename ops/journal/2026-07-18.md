# 2026-07-18 — pre-ship production pass (setup + review + diagnosis)

Session goal (Illya): last pass before production. Review everything, fix the
reported design issues, review the triage/writing cadences for cost + quality,
strengthen source/free-version policy, build a self-improving log system, and
schedule it for 4am.

## Done this session
- **Scheduled** the nightly self-improvement pass via `runat` (`eclecta-nightly`,
  04:00 local, cwd = this repo).
- **Ran** a 6-dimension adversarial production review (Workflow
  `wf_e75e6ea7-c5a`). 19 findings confirmed; full output in
  `tasks/we1xrqjcj.output`. Folded into `IMPROVEMENTS.md`.
- **Captured** every page type at desktop + mobile and root-caused the visual
  bugs firsthand (masthead baseline, footer padding override, about compact
  header).
- **Built** this `ops/` system.

## Shipped + DEPLOYED (two commits on main, CI green)
- Site (246 unit + 110 e2e): masthead centering, Title-Case editions,
  dateline→current-edition link, mobile footer gutter + zero horizontal overflow
  (360–390px), unified full masthead everywhere, novelty CSS clamp, new
  `layout.spec.ts` + digest-header + sitemap + OG e2e tests.
- Pipeline (2650 pytest): short-excerpt triage/judge (`MAX_JUDGE_CHARS`) at
  `effort=low`, novelty persistence clamp + tighter prompts, per-kind digest
  effort (high daily/weekly, max monthly+), Sonnet-5/Opus routing + PRICING in
  the example config, dead-knob cleanup, runbook cron fix, provenance/free-read
  policy, editorial-policy §1 de-personalized.
- CI: `pr.yml` now runs the pipeline pytest suite (paths-filtered).
- **Live pipeline config** (his go): `tier_overrides={}`, triage/judge/deep →
  `claude-sonnet-5`, write/digest → `claude-opus-4-8`, `digest.local` →
  `qwen2.5:14b`. Backup: `signal.json.bak-2026-07-18-sonnet5`. Config-only; the
  worker reloads per job (no restart done).

## Nightly pass run early (Illya: "we have quota, don't wait till 4am")
- **Health:** tree clean; `eclecta.co` → 200; newest editions present (daily
  2026-07-17, weekly 2026-W29, monthly 2026-06, quarterly 2026-Q2).
- **Editorial quality:** the two newest editions are clean — zero banned words,
  zero em-dashes; daily ≈801 words (target 300–700, slightly long), weekly ≈1358
  (target 700–1200, slightly long). Prose quality holds.
- **Finding (open):** the live `picks.json` still has 300+ char `novelty` values
  (lead is 314 ch). The site CSS clamp bounds the *display* to 2 lines, so the
  reader-facing standfirst is already fine, but the DATA is long because the
  **deployed** pipeline copy (`~/.local/state/signal/app/signalpipe`) doesn't yet
  have the repo's novelty clamp. Permanent fix = sync the pipeline code + worker
  kickstart (a pipeline deploy, Illya's explicit go), after which the next
  publish writes short novelties. Not a service action for this pass.

## Open / next pass
- **Pipeline code deploy** to apply the novelty clamp + short-excerpt + effort
  changes to the LIVE worker: rsync `pipeline/signalpipe` → deployed copy, then
  `launchctl kickstart -k gui/$UID/io.starikov.signal.worker` (Illya's go —
  code changes need a worker restart, unlike config which reloads per job).
- Daily/weekly editions running slightly over their word targets — watch whether
  the tighter prompts pull them in.

## Session 2 — self-learning layer (branch `feat/self-learning-layer`)

Owner asked for the last-minute additions: professional footer, plus a real
self-learning system (eval sets, a "featured" bar that hardens over time, "what
matters now/next", a growing Library/wiki). Brainstormed → spec → plan → built
all three phases, each committed green, on `feat/self-learning-layer`.

- **Footer** → "Colophon" (kept the Art. 50 machine declaration).
- **Eval** (`eval.py` + `signal eval …` + `eval/`): gold corpus from the DB
  read-only, judge replay on the free local backend ($0), agreement/precision/
  recall/MAE metrics. 10 tests.
- **Adaptive bar** (`adaptive.py`): bounded ratcheting percentile at every funnel
  gate, **default OFF**, floor never starves, effective bar in `runs.stats`. 9
  tests. Regression guard: disabled = byte-identical (full suite green).
- **Momentum** (`momentum.py` + daily job): `kb/momentum.json` + opt-in
  `topic_match` multiplier (default off). 8 tests.
- **Library** (`library.py` + daily job + `/library/`): non-person entity wiki,
  grows a few/run; public index + entity pages in nav/footer; 8 real seed
  entities generated from live coverage, zero archive links. 5 unit + 4 e2e.
- **Loop**: `ops/self-improve.md` step 4 now runs eval/momentum/library; runbook
  + LEARNINGS + IMPROVEMENTS updated.

Suites: pipeline **2694** pytest; site **246** unit + **116** e2e. Branch pushed;
NOT merged to main and NOT deployed (owner's call). Adaptive + momentum ship
disabled — flipping them live is a staged config change for Illya to make after
watching a few cycles.
