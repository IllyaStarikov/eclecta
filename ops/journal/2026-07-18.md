# 2026-07-18 — pre-ship production pass (setup + review + diagnosis)

Session goal (Illya): last pass before production. Review everything, fix the
reported design issues, review the triage/writing cadences for cost + quality,
strengthen source/free-version policy, build a self-improving log system, and
schedule it for 4am. Session paused early to conserve usage (limit resets 3:10am);
the actual fixes are queued for the nightly pass.

## Done this session
- **Scheduled** the nightly self-improvement pass via `runat` (`eclecta-nightly`,
  04:00 local, cwd = this repo). Session armed to drive it; caffeinate already
  running. Note: a second 4am task (`starikov-io-tasks-9-20`, a different repo)
  shares the queue — whichever session fires first runs both, deduped by the
  queue's mark-running guard.
- **Ran** a 6-dimension adversarial production review (Workflow
  `wf_e75e6ea7-c5a`). 19 findings confirmed after verification; full output in
  `tasks/we1xrqjcj.output`. Folded into `IMPROVEMENTS.md` §B. (Many verify agents
  errored on the session limit — the confirmed set is still solid; a couple of
  raw findings went unverified and are noted as such.)
- **Captured** every page type at desktop (1440) + mobile (390), before-state,
  and root-caused the three visual bugs firsthand:
  - Masthead: `align-items: baseline` bottom-anchors the kicker → looks low.
  - Footer mobile clip: `.foot`'s `padding … 0 …` overrides `.wrap` gutter → 0
    left padding on mobile.
  - About header: it uses `compactHeader`, unlike the front page.
- **Built** this `ops/` system: `IMPROVEMENTS.md` (backlog), `self-improve.md`
  (runbook), `LEARNINGS.md`, `README.md`, and this journal.

## Diagnosed but NOT yet applied (queued in IMPROVEMENTS.md §A)
Masthead centering, Title-case edition labels, dateline→latest-edition link,
mobile footer padding, header unification, novelty clamp (site+pipeline),
original-source/free-version emphasis, de-personalizing `editorial-policy.md` §1
(names Google/Garmin in a public repo). Brand kicker/tagline → needs Illya's pick.

## Open / next pass
- Execute `IMPROVEMENTS.md` §A (owner-requested) first, then the High findings
  (MAX_JUDGE_CHARS, pr.yml pytest gate). Keep build + all suites green.
- Ask Illya the brand question (kicker + tagline options in §A7) and the `[?]`
  routing/cap decisions before touching those.
- Reminder: repo edits only; no live-service changes without his go.
