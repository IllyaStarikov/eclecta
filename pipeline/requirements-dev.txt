# Dev/test dependencies for the signalpipe pytest suite.
# Installed in CI on top of requirements.txt so the hermetic suite can import
# every module (many optional runtime deps are imported lazily inside
# functions, but a few integration tests exercise those paths directly).
-r requirements.txt

pytest>=7.4,<9
pytest-cov>=4.1
coverage>=7.4
hypothesis>=6.100          # property tests; guarded by an import-skip if absent
ruff>=0.4

# Runtime deps that integration tests import directly (unit tests rely on the
# lazy in-function imports and don't need these):
httpx>=0.27
jinja2>=3.1
markdown>=3.5
anthropic>=0.40            # backend_api integration tests (mocked; no network)
fastapi>=0.110            # server TestClient integration tests
apscheduler>=3.10,<4      # worker scheduler integration tests
starlette>=0.37           # FastAPI TestClient dependency
