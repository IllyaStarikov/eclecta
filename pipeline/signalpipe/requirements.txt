# Signal pipeline deps beyond what the repo already uses.
# Core (anthropic, fastapi, uvicorn, httpx, lxml, jinja2) are already installed.
feedparser>=6.0.12
trafilatura>=1.8
readability-lxml>=0.8.1
APScheduler>=3.10,<4
defusedxml>=0.7.1

